apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
  namespace: notes-app
type: Opaque
data:
  # Base64 encoded values - replace with your actual values
  # To encode: echo -n "your-password" | base64
  postgres-password: mysecretpassword  # used for backend and db.